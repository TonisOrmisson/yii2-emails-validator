build: { tests: false }
checks:
  php: true
coding_style:
  php:
    spaces:
      around_operators:
        relational: false
        concatenation: true
filter:
  excluded_paths:
    - 'tests/*'
    - 'vendor/*'